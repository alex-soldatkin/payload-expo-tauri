# Mobile Expo Preview

Simple Expo app that calls `/api/admin-schema` and renders a summary.

- Update the base URL to point at your Payload server.
- Provide a JWT token if you are not using cookies.
