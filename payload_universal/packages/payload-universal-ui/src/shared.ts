/**
 * @payload-universal/ui/shared — Web entry point for shared utilities.
 *
 * Re-exports from @payloadcms/ui/shared on web.
 */
export {
  EntityType,
  groupNavItems,
  formatAdminURL,
  getVisibleEntities,
} from '@payloadcms/ui/shared'
